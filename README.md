# RecordU

![RecordU](icons/icon128.png)

看到让你咯噔一下的内容，5 秒记下你自己的话。每周回顾，积累成属于你的认知清单。

Chrome / Chromium 扩展（Manifest V3），帮助你在浏览网页时快速捕获阅读感触、标记单词，并在原页面高亮标注，方便日后回顾与筛选。

## 功能

- **选区捕获** — 选中页面文字后，点击浮动按钮 **i**（想法）或右键菜单，写下你的感触
- **标记单词** — 选区旁点 **w**，写下释义或备注；该词会在**所有网站**自动高亮
- **截图批注** — 框选页面区域截图后写下感触（适合 PDF、图表等无法划词的页面）
- **快捷键** — 快速捕获：Windows/Linux `Alt+S`，Mac `⌥S`（Option+S）；截图批注：Windows/Linux `Alt+Shift+S`，Mac `⌥⇧S`（Option+Shift+S）
- **原文高亮** — 文字感触保存后原文自动高亮（暖金色）；再次打开同一页面时自动恢复，点击高亮可查看和编辑
- **单词高亮** — 已标记单词在任意网页全文匹配高亮（蓝色）；默认支持英语词形变体；点击可查看释义
- **回顾页** — 待回顾（按时间 / 按网站）、单词时间线；支持日期筛选、编辑与删除
- **设置** — 高亮颜色、单词精准/变体匹配；支持 ZIP 完整备份与恢复
- **导出 Markdown** — 一键导出当前筛选结果，便于归档或二次整理（不含单词列表与截图文件）

## 安装

1. 克隆或下载本项目
2. 打开 Chrome（或夸克等 Chromium 浏览器），访问扩展管理页
3. 开启 **开发者模式**
4. 点击 **加载已解压的扩展程序**，选择项目根目录

安装完成后，浏览器工具栏会出现扩展图标。

## 使用

### 方式一：选区捕获（推荐，支持高亮）

1. 在任意网页上选中一段文字
2. 选区旁出现两个浮动按钮：**i**（记下感触）与 **w**（标记单词）
3. 点 **i**，在面板中写下你的感触，点击 **保存**

保存后，选中的原文会立即高亮。之后再次访问该页面，高亮会自动恢复；**点击高亮**可查看并编辑你的感触。

高亮用 **TextQuote（原文 + 前后文）** 定位，失败时再回退 **TextPosition（全文偏移）**；跨标签选区会按文本节点分片包裹，避免拆坏 DOM。动态页面会通过 MutationObserver 与 `history` 变化持续尝试恢复。

> 高亮依赖保存时的选区锚点（`anchor`）。通过弹窗直接记录、未选中文字时不会生成高亮。若页面正文被大幅改写，高亮可能无法恢复。
>
> **PDF 预览页**：浏览器内置 PDF 查看器无法监听划词。请用 **截图批注**（Mac：`⌥⇧S`，其他：`Alt+Shift+S`），或选中后右键「记下感触」（若菜单可用）。
>
> **不兼容旧版数据**：旧记录里的 `context` / `prefix` / `suffix` 字段已废弃。升级后请先导出备份，再清空本地数据或重新捕获，旧条目不会再显示页面高亮。

### 方式二：标记单词

1. 选中要学的单词或短语
2. 点击浮动按钮 **w**
3. 可选填写释义或备注，保存

该词会在**所有 http(s) 页面**按词匹配并高亮（拉丁词整词、大小写不敏感；中日韩等按精确子串）。再次标记同一词会更新备注，不会重复条目。回顾页「单词」分区可编辑释义或删除（删除后全站取消高亮）。

### 方式三：截图批注

1. 按快捷键（Mac：`⌥⇧S` / Option+Shift+S；Windows/Linux：`Alt+Shift+S`），或打开弹窗点击 **截图批注**
2. 拖拽框选页面区域，松手后自动截取
3. 写下感触并保存

截图以 JPEG 压缩后存入本地 IndexedDB，回顾页可查看缩略图。截图批注**不会**在原页恢复高亮框。

### 方式四：快捷键 / 弹窗

按快捷键（Mac：`⌥S` / Option+S；Windows/Linux：`Alt+S`）或点击扩展图标，打开快速捕获弹窗。适合快速记下一闪而过的想法，会关联当前页面标题和 URL，但不包含原文选区。

### 方式五：右键菜单

选中文字后，右键选择 **记下感触**，效果同方式一的感触捕获。

### 回顾与整理

点击弹窗中的 **回顾 ↗**，或在扩展内打开回顾页。默认进入本周队列：

| 功能 | 说明 |
|------|------|
| 待回顾 | 顶栏入口；其下可切换 **按时间**（左列表右详情）/ **按网站**（左站聚合右条目） |
| 按时间 / 按网站 | 待回顾的两种展示方式 |
| 单词 | 按日时间线网格：词、释义、来源；可编辑释义、删除 |
| 设置 | 高亮颜色、单词匹配模式（变体 / 精准）；ZIP 备份与恢复；保存后全站立即生效 |
| 本周 / 更早 / 全部 | 日期范围筛选（仅待回顾显示） |
| 导出 | 导出当前展示与日期范围下的 Markdown（截图条仅标注「截图批注」，不内嵌图片；不含单词） |

**回顾页快捷键**（待回顾·按时间 / 单词）：

| 按键 | 作用 |
|------|------|
| `j` / `↓` | 下一条 |
| `k` / `↑` | 上一条 |
| `e` | 编辑当前条目 |
| `d` / `Backspace` | 删除当前条目 |
| `o` | 在新标签页打开原文链接 |

建议每周花约 20 分钟回顾：把还让你有感觉的留着，其余删掉。

## 项目结构

```
.
├── manifest.json          # 扩展配置
├── background.js          # 后台服务：IndexedDB、设置、右键菜单、截图、消息路由
├── content.js             # 内容脚本：选区浮层、感触/单词捕获、高亮
├── popup.html / popup.js  # 快速捕获弹窗
├── review.html            # 回顾页入口
├── review/
│   ├── main.js            # 回顾页入口与路由
│   ├── state.js           # 模式、日期范围、焦点状态
│   ├── queue-view.js      # 待回顾队列视图
│   ├── browse-view.js     # 按网站浏览视图
│   ├── words-view.js      # 单词列表视图
│   ├── settings-view.js   # 全局设置与数据备份
│   ├── backup.js          # ZIP 备份打包 / 恢复
│   ├── split-view.js      # 左列表 + 右详情布局
│   ├── keys.js            # 键盘快捷键
│   ├── export-md.js       # Markdown 导出
│   └── action-icons.js    # 操作按钮图标
├── shared/
│   ├── db.js              # IndexedDB 封装与迁移（captures / words）
│   ├── api.js             # 与 background 通信
│   ├── settings.js        # 全局设置默认值与规范化
│   ├── captures.js        # 捕获记录工具函数
│   ├── capture-theme.js   # 页内浮层和紙主题 CSS
│   ├── dom.js             # DOM 工具
│   ├── time.js            # 日期与时间格式化
│   └── favicon.js         # 回顾页站点图标
├── vendor/
│   └── fflate.js          # ZIP 压缩（备份用）
├── modal.js               # 确认对话框
├── styles.css             # 弹窗与回顾页样式
└── icons/                 # 扩展图标
```

## 数据存储

感触、截图与单词保存在浏览器本地 **IndexedDB**（扩展源）；高亮颜色等全局设置保存在 **chrome.storage.local**。不会上传到任何服务器。首次启动会把旧版 `chrome.storage.local` 中的捕获数据自动迁入 IndexedDB。

**卸载扩展**、清除扩展站点数据，或换路径加载导致扩展 ID 变化时，本地数据会丢失。同一扩展 ID 下「重新加载」通常不会丢数据。重要内容请到回顾页 **设置 → 数据备份** 下载 ZIP（含感触、截图二进制、单词、favicon 与设置）；恢复前会清空当前本地数据并以备份为准。页头 **导出** Markdown 仍只作可读文本归档（不含截图文件与单词列表）。

## 权限说明

| 权限 | 用途 |
|------|------|
| `storage` | 全局设置、迁移标记与旧数据迁出 |
| `contextMenus` | 右键「记下感触」菜单 |
| `activeTab` | 获取当前标签页、截取可见区域 |
| `scripting` | 必要时向当前页注入内容脚本以启动截图 |
| `<all_urls>` | 在任意网页注入内容脚本，实现选区捕获、单词高亮与感触高亮 |

## 开发

本项目为纯 JavaScript，无构建步骤。修改代码后，在扩展管理页点击扩展的 **重新加载** 即可生效。

回顾页和弹窗需在已加载的扩展环境中打开（通过扩展图标或 `review.html`），不能单独用静态服务器预览完整功能。

## 更新日志

### 0.5.0

- **单词高亮词形**：默认变体匹配（双向词干，如 overwhelm ↔ overwhelmed / overwhelming）；设置可改为精准匹配
- **设置**：回顾页可切换「变体匹配 / 精准匹配」，保存后全站立即生效

### 0.4.1

- **数据备份 / 恢复**：回顾页设置支持下载 ZIP 完整备份（感触、截图、单词、favicon、设置）与整库恢复

### 0.4.0

- **标记单词**：划词后双按钮浮层（**i** 感触 / **w** 单词）；可写释义并保存；同词去重更新
- **全站单词高亮**：已标记词在所有网页匹配高亮；点击查看释义；回顾页「单词」可编辑 / 删除
- **全局设置**：回顾页「设置」可配置感触与单词高亮颜色（含预设），保存后立即全站生效
- **存储**：IndexedDB 新增 `words` store（库版本 2）

### 0.3.1

- **新图标**：和紙风格灯泡图标；弹窗与回顾页标题旁展示品牌图标

### 0.3.0

- **和紙 UI**：捕获弹窗、页内浮层与回顾页统一为纸色 / 墨色视觉；不再依赖 Google Fonts，改用系统字体
- **回顾页信息架构**：导出与模式导航分离；模式与日期范围同一行；进度只显示条数
- **捕获浮层**：标题栏可拖拽移动；右键菜单文案改为「记下感触」
- **站点图标**：待回顾列表与详情旁也显示 favicon（与按网站视图一致）

### 0.2.2

- **截图预览**：回顾页点击截图可全屏预览，支持滚轮 / 按钮放大缩小、拖拽平移

### 0.2.1

- **回顾页编辑**：待回顾 / 按网站视图均可编辑单条感触；待回顾模式支持快捷键 `e`

### 0.2.0

- **截图批注**：框选页面区域截图后写下感触，适合 PDF、图表等无法划词的页面
- **快捷键**：Mac `⌥⇧S`（Option+Shift+S）/ Windows·Linux `Alt+Shift+S` 启动截图；弹窗增加「截图批注」入口
- **IndexedDB 存储**：感触、截图 Blob、favicon 全部迁入本地 IndexedDB；首次启动自动从旧版 `chrome.storage.local` 迁移
- **回顾页**：支持展示截图缩略图；Markdown 导出对截图条标注「截图批注」（不内嵌图片）
- **兼容**：content script 不再直连 storage，统一经 background 读写

### 0.1.0

- 选区捕获、浮动按钮与右键「记下感触」
- 原文高亮（TextQuote / TextPosition）与页面恢复
- 快捷键 `Alt+S` / Mac `⌥S` 快速捕获弹窗
- 回顾页：待回顾队列、按网站浏览、日期筛选、删除整理
- 导出 Markdown
- 数据保存在 `chrome.storage.local`
